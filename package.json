{
  "name": "qa-portfolio-bdd-playwright-completo",
  "version": "1.0.0",
  "description": "Portf\u00f3lio QA com Playwright e BDD (Cucumber + Gherkin)",
  "scripts": {
    "install-browsers": "npx playwright install",
    "test": "npx cucumber-js --require ./features/support/*.js --require ./features/step_definitions/**/*.js --format html:reports/report.html --format progress"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "@cucumber/html-formatter": "^20.0.0",
    "playwright": "^1.40.0"
  }
}